import Wrapper from "../Wrapper";

const index = () => {
    return (
        <Wrapper>
             <h1 className="text-success">Page d'Accueil</h1>
        </Wrapper>
    );
};

export default index;